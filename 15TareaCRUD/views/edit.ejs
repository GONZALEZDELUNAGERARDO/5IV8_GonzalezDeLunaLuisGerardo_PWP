<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Datos de Las Inspecciones</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Bebas+Neue&family=Oswald:wght@400;600;700&family=Roboto:ital,wght@0,400;0,500;1,400&family=Roboto+Condensed:ital,wght@0,400;1,400&display=swap" rel="stylesheet">

</head>
<body>

    <h1>Actualizar Registro de Bitácora</h1>
<form action="/bitacora/update/<%= registro.id %>" method="POST">
    <label for="equipo">Id del Equipo:</label>
    <input type="text" id="equipo" name="id_equipo" value="<%= registro.id_equipo %>" required>

    <label for="fluido">Tipo de Fluido:</label>
    <input type="text" id="fluido" name="tipo_fluido" value="<%= registro.tipo_fluido %>" required>

    <label for="fecha">Fecha de Cambio:</label>
    <input type="date" id="fecha" name="fecha_cambio" value="<%= registro.fecha_cambio.toISOString().slice(0,10) %>" required>

    <label for="cantidad">Cantidad (litros):</label>
    <input type="number" step="0.01" id="cantidad" name="cantidad" value="<%= registro.cantidad %>" required>

    <label for="muestra">Muestra de Análisis:</label>
    <select id="muestra" name="muestra_analisis" required>
        <option value="Si" <%= registro.muestra_analisis === 'Si' ? 'selected' : '' %>>Sí</option>
        <option value="No" <%= registro.muestra_analisis === 'No' ? 'selected' : '' %>>No</option>
    </select>

    <label for="resultados">Resultados del Análisis:</label>
    <textarea id="resultados" name="resultados_analisis" rows="3"><%= registro.resultados_analisis %></textarea>

    <label for="proximo">Próximo Cambio:</label>
    <input type="date" id="proximo" name="proximo_cambio" value="<%= registro.proximo_cambio.toISOString().slice(0,10) %>" required>

    <button type="submit">Actualizar</button>
</form>
    <a href="/">Regresar al Formulario General</a>
    
</body>
</html>