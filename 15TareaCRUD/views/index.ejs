<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/15TareaCRUD/css/style.css">
    <title>Bitácora de Lubricación</title>
</head>
<body>
    <h1>Bitácora de Lubricación</h1>

    <form action="/bitacora" method="POST">
        <label for="equipo">Id del Equipo: </label>
        <input type="text" id="equipo" name="id_equipo" required>

        <label for="fluido">Tipo de Fluido: </label>
        <input type="text" id="fluido" name="tipo_fluido" required>

        <label for="fecha">Fecha de Cambio: </label>
        <input type="date" id="fecha" name="fecha_cambio" required>

        <label for="cantidad">Cantidad (litros): </label>
        <input type="number" step="0.01" id="cantidad" name="cantidad" required>

        <label for="muestra">Muestra de Análisis: </label>
        <select id="muestra" name="muestra_analisis" required>
            <option value="Si">Sí</option>
            <option value="No">No</option>
        </select>

        <label for="resultados">Resultados del Análisis: </label>
        <textarea id="resultados" name="resultados_analisis" rows="3"></textarea>

        <label for="proximo">Próximo Cambio: </label>
        <input type="date" id="proximo" name="proximo_cambio" required>

        <button type="submit">Registrar Cambio</button>
    </form>

    <ul>
      <% bitacora.forEach(function(registro){ %>
        <li>
          <div class="registro">
            <strong><%= registro.fecha_cambio %></strong>  
            — Equipo: <%= registro.id_equipo %>  
            — Fluido: <%= registro.tipo_fluido %>  
            — Cantidad: <%= registro.cantidad %>  
            — Muestra: <%= registro.muestra_analisis %>  
            — Resultados: <%= registro.resultados_analisis %>  
            — Próximo Cambio: <%= registro.proximo_cambio %>
          </div>
          <div class="actions">
            <a href="/bitacora/edit/<%= registro.id %>">Editar</a>
            <a href="/bitacora/delete/<%= registro.id %>">Eliminar</a>
          </div>
        </li>
      <% }) %>
    </ul>
</body>
</html>